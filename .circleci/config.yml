version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:21.6.1
    steps:
      - checkout
      - run: echo "npm installing"   
      - run: npm install
      - run: CI=true npm run build
  test:
    docker:
      - image: cimg/node:21.6.1
    steps:
      - checkout
      - run: echo "testing stuff"   
      - run: npm install
      - run: npm test
  hithere:
    docker:
      - image: cimg/node:21.6.1
    steps:
      - checkout
      - run: echo "Hello!"
workflows:
  version: 2
  build_test_lint:
    jobs:
      - build
      - hithere
      - test:
          requires:
            - hithere

